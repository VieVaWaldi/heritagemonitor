// Shared border styling for the ActionBar's three segments (SearchBar,
// EntitySelector, CorpusSelector) — one place to tune width/color for all of
// them instead of three separate hardcoded copies.
// Idk maybe this is a bad place for this, reevaluate later
export const ACTION_BAR_BORDER_WIDTH = 1.6
export const ACTION_BAR_BORDER_COLOR = 'primary.main'
export const ACTION_BAR_BORDER_HOVER_COLOR = 'primary.dark'
