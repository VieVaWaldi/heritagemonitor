import type {NextConfig} from 'next'

const nextConfig: NextConfig = {
    // Nothing custom yet. Turbopack is the default dev/build engine as of
    // Next.js 16 — no flag needed. Revisit when MUI's App Router integration
    // or a future dependency requires specific config here.
}

export default nextConfig